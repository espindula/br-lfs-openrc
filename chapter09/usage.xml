<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
  "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../general.ent">
  <!ENTITY rc-hwclock         SYSTEM "../appendices/rc-hwclock.script">
  %general-entities;
]>

<sect1 id="ch-config-usage" revision="openrc">
  <?dbhtml filename="usage.html"?>

  <title>Uso e Configuração do OpenRC</title>

  <indexterm zone="ch-config-usage">
    <primary sortas="a-Usage">Uso</primary>
    <secondary>uso</secondary>
  </indexterm>

  <sect2>
    <title>How Does OpenRC Work?</title>

    <para>This version of LFS uses a special booting facility named OpenRC, which
starts services according to a series of <emphasis>run
levels</emphasis>. These runlevels are slightly different from init systems
like SysVinit. While these runlevels exist, there is much less complexity
involved with OpenRC services. The process doesn't require bootscripts.</para>
  
    <para>Existe um procedimento alternativo de inicialização chamado
<command>systemd</command>. Nós não mais discutiremos esse processo de
inicialização aqui. Para uma descrição detalhada, visite-se <ulink
url="https://www.linux.com/training-tutorials/understanding-and-using-systemd/"/>.</para>

    <para>OpenRC's run level scheme has five run levels that are the most important,
with the ability to add more.  Each one of the five corresponds to certain
modes the computer is in, like if it's booting, shutting down, or has no
networking. Here are the descriptions of the different run levels as they
are implemented in LFS:</para>

    <itemizedlist>
      <listitem>
        <para>
          sysinit: brings up and mounts kernel virtual filesystems
        </para>
      </listitem>
      <listitem>
        <para>
          boot: starts up important services like setting the time and checks the
filesystem
        </para>
      </listitem>
      <listitem>
        <para>
          nonetwork: default without networking
        </para>
      </listitem>
      <listitem>
        <para>
          default: where most services should be, like setting up audio services
        </para>
      </listitem>
      <listitem>
        <para>
          shutdown: kills all processes and remounts <filename
class="directory">/</filename> in read-only mode
        </para>
      </listitem>
    </itemizedlist>

    <para>
      Each service has dependency information, so a service on a given run level
will be started at the correct time if it has correct dependency
information.
    </para>

  </sect2>

  <sect2 id="conf-openrc" role="configuration">
  <title>Configurando OpenRC</title>

  <indexterm zone="conf-openrc">
    <primary sortas="a-OpenRC">OpenRC</primary>
    <secondary>configurando</secondary>
  </indexterm>

  <para>Whenever a system is started, <command>agetty</command> is ran for a number
of <literal>tty</literal> devices. OpenRC provides an
<command>agetty</command> service but doesn't start it for each
<literal>tty</literal> that's needed. Create and enable those services:</para>

<screen><userinput>rm -vf /etc/runlevels/default/agetty*
for i in $(seq 1 6); do
  ln -sfv agetty /etc/init.d/agetty.tty$i
  rc-update add agetty.tty$i default
done</userinput></screen>

  <para>These <literal>tty</literal> services are started on the
<literal>default</literal> run level.</para>

  </sect2>

  <sect2 id="ch-config-udevd">
    <title>Creating the Udevd Service</title>

    <indexterm zone="ch-config-udevd">
      <primary sortas="d-udevd">udevd</primary>
    <secondary>configurando</secondary></indexterm>

  <para>OpenRC doesn't ship services for <command>udevd</command>, leading to kernel
modules not being loaded. Create and enable them now:</para>

<screen><userinput>cat &gt; /etc/init.d/udevd &lt;&lt; "EOF"
<literal>#!/sbin/openrc-run
# Starts the udevd daemon.
# Authors: Zeckma - zeckma.tech@gmail.com
# Version: LFS 12.5

description="starts the udevd daemon"

command=/sbin/udevd
command_background=yes
pidfile="/run/${RC_SVCNAME}.pid"

depend() {
  after localnet
  after modules
}

stop() {
  ebegin "Stopping ${RC_SVCNAME}"
  /bin/udevadm control --exit
  eend $?
}</literal>
EOF

cat &gt; /etc/init.d/udev-trigger &lt;&lt; "EOF"
<literal>#!/sbin/openrc-run
# Coldplugs devices and settles the udevd daemon.
# Authors: Zeckma - zeckma.tech@gmail.com
# Version: LFS 12.5

description="coldplugs devices"

depend() {
  after udevd
  provides dev
  keyword -shutdown
}

start() {
  ebegin "Coldplugging devices"

  /bin/udevadm trigger --action=add    --type=subsystems
  /bin/udevadm trigger --action=add    --type=devices
  /bin/udevadm trigger --action=change --type=devices

# Wait for udevd to process the uevents we triggered
  /bin/udevadm settle --timeout=5

# If any LVM based partitions are on the system, ensure they
# are activated so they can be used.
  if [ -x /sbin/vgchange ]; then /sbin/vgchange -a y >/dev/null; fi
}</literal>
EOF

for i in udev{d,-trigger}; do
  chmod 755 /etc/init.d/$i
  rc-update add $i sysinit
done</userinput></screen>

    <para>
      These services should load most if not all modules automatically. If you
need to load a given module but it isn't being loaded, pass the module name
to the <filename>/etc/conf.d/modules</filename> configuration file.
    </para>

  </sect2>

  <sect2 id="ch-config-clock">
    <title>Configurando o Relógio do Sistema</title>

    <indexterm zone="ch-config-clock">
      <primary sortas="d-setclock">setclock</primary>
    <secondary>configurando</secondary></indexterm>

  <para>OpenRC provides a <command>hwclock</command> service which reads from the
hardware clock. It then sets the time. This time is obtained from either
applying timezone data from <filename>/etc/localtime</filename>, or it
simply doesn't do anything and displays the time as-is. In order to know
what to do with the hardware time, it needs to decide whether the time is
UTC or local.  It has no way to do this on its own, so a configuration file
is needed.  OpenRC provides one already, and should be edited.</para>

  <para>Se você não conseguir lembrar se o relógio do hardware está ou não
configurado para UTC, descubra executando o comando <userinput>hwclock
--localtime --show</userinput>. Isso mostrará o que é a hora atual de acordo
com o relógio do hardware. Se essa hora corresponder à que o seu relógio
diz, então o relógio do hardware está configurado para hora local. Se a
saída gerada originária do <command>hwclock</command> não for a hora local,
as chances são as de que ele esteja configurado para hora UTC. Verifique
isso adicionando ou subtraindo a número apropriado de horas para o seu fuso
horário à (da) hora mostrada pelo <command>hwclock</command>. Por exemplo,
se você estiver atualmente no fuso horário MST, o qual é conhecido também
como GMT -0700, adicione sete horas à hora local.</para>

  <para>Once you know the hardware time uses, edit
<filename>/etc/conf.d/hwclock</filename> as necessary. There are plenty of
comments explaining what to edit.</para>

  </sect2>

  <sect2 id="ch-config-console">
  <?dbhtml filename="console.html"?>

  <title>Configurando o Console do Linux</title>

  <indexterm zone="ch-config-console">
    <primary sortas="d-console">console</primary>
    <secondary>configurando</secondary>
  </indexterm>

  <para>Esta seção discute como configurar os serviços
<command>consolefont</command> e <command>keymap</command> que configuram a
fonte do console e o mapa de teclado. Se caracteres não-ASCII (por exemplo,
o sinal de copirraite, o sinal da libra britânica e o símbolo do Euro) não
forem usados e o teclado for um dos Estados Unidos da América do Norte,
muito desta seção pode ser ignorada. Sem os arquivos de configuração, os
serviços <command>consolefont</command> e <command>keymap</command> usarão
os padrões.</para>

  <para>The <command>consolefont</command> service reads the
<filename>/etc/conf.d/consolefont</filename> file for configuration
information. Decide which screen font will be used. Search <filename
class="directory">/usr/share/consolefonts</filename> for valid fonts.</para>

  <para>O serviço <command>keymaps</command> lê o arquivo
<filename>/etc/conf.d/keymaps</filename> para informações de
configuração. Decida qual mapa de teclado será usado. Vários HOWTOs
específicos de cada idioma também podem ajudar com isso; veja-se <ulink
url="https://tldp.org/HOWTO/HOWTO-INDEX/other-lang.html"/>. Se ainda em
dúvida, procure no diretório <filename
class="directory">/usr/share/keymaps</filename> por mapas válidos de
teclado.</para>

  <para>For both <command>consolefont</command> and <command>keymaps</command>,
configuration files exist for both and have comments that give good
information about what to provide to each parameter.</para>

  
  <!-- Added because folks keep posting their console file with X questions
  to blfs-support list -->
<note>
    <para>Os arquivos de configuração para o console somente controlam a localização
do console de texto do Linux. Eles não tem nada a ver com configurar o
esquema adequado de teclado e fontes de terminal no Sistema de Janelas X;
com sessões do ssh; ou com um console serial.</para>
  </note>

  </sect2>

  <sect2 id="ch-config-agetty">
    <title>Configurando Agetty</title>

    <para>
      If you don't want the boot logs to be cleared on the first TTY, which Agetty
will clear them by default, add <option>--noclear</option> to
<literal>agetty_options</literal> in the
<filename>/etc/conf.d/agetty</filename> file.
    </para>

  </sect2>

  <sect2 id="ch-config-rc">
    <title>O Arquivo rc.conf</title>

    <indexterm zone="ch-config-rc">
      <primary sortas="a-rc.conf">rc.conf</primary>
    </indexterm>

    <para>The <filename>/etc/rc.conf</filename> file contains settings that control
how OpenRC starts and operates. For example, you can tell OpenRC to start
services in parallel, or you can specify what run level you want the system
to run at. In most cases, you won't need to edit this file.</para>

  </sect2>

  <sect2 id="ch-config-other-rc">
    <title>Other OpenRC Configuration Files</title>

    <para>Many other services have configuration files in <filename
class="directory">/etc/conf.d</filename> and should match the service name
found in <filename class="directory">/etc/init.d</filename>. If a service
gives you any trouble, there may be a configuration file for it already, and
should have comments provided.</para>

  </sect2>
</sect1>
